<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="?android:attr/listPreferredItemHeight"
    android:padding="@dimen/chat_img_padding">

    <ImageView
        android:id="@+id/chat_img"
        android:layout_width="@dimen/chat_img_width"
        android:layout_height="fill_parent"
        android:layout_alignParentBottom="true"
        android:layout_alignParentTop="true"
        android:layout_marginRight="@dimen/chat_img_padding"
        android:contentDescription="@string/chat_avatar"
        android:scaleType="fitStart" />

    <TextView
        android:id="@+id/chat_name"
        android:layout_width="fill_parent"
        android:layout_height="@dimen/chat_name_height"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:layout_toRightOf="@id/chat_img"
        android:paddingLeft="@dimen/chat_text_padding"
        android:ellipsize="marquee"
        android:singleLine="true"
        android:gravity="center_vertical"
        android:textColor="@color/chat_text" />

    <TextView
        android:id="@+id/chat_msg"
        android:layout_width="fill_parent"
        android:layout_height="@dimen/chat_msg_height"
        android:layout_below="@id/chat_name"
        android:layout_alignParentBottom="true"
        android:layout_toRightOf="@id/chat_img"
        android:paddingLeft="@dimen/chat_text_padding"
        android:gravity="center_vertical"
        android:textColor="@color/chat_text" />

</RelativeLayout>